# 项目开发约束

## 界面图标

- 新增或修改界面时统一使用 Lucide 图标库，不使用 Emoji、Unicode 字符或手绘 SVG 代替功能图标。
- 本项目通过 `app/templates/icons/lucide.html` 复用官方 `lucide-static` SVG 子集，固定版本并保留同目录的许可证；新增图标从同版本官方包导入。
- Jinja 模板使用 `icon(name)` 和 `sprite()`，避免重复定义图标路径。图标随项目本地提供，不依赖运行时外部 CDN。
- 图标使用 `currentColor` 和统一线宽，适配深浅主题。装饰图标设置 `aria-hidden="true"`，纯图标按钮提供中文 `aria-label`，点击区域至少 44×44px。

## 红线：容器平台与托管站点必须解耦

- 本仓库仅管理通用平台、管理后台、SDK、skill、部署协议及通用测试。线上容器的私有源码快照是站点源码的权威版本库，本地设备只是临时开发工作副本。
- 禁止将真实站点源码、业务 Schema、种子数据、站点测试或站点专属规则加入本仓库，包括 `sites/`、`examples/`、测试夹具和任何改名目录；不得以“示例”“备份”绕过红线。通用测试只能使用人工构造的最小数据。
- 禁止在平台中硬编码真实站点 slug、项目字段、业务数据格式或界面交互。站点需要能力时，先抽象通用协议，并用合成数据验证；兼容旧协议必须隔离并说明边界。
- 收到站点开发请求时，agent 必须自动使用 microsite-container skill：确认 slug → 拉取到本仓库之外的新目录 → 读取本站 AGENTS.md → 开发并测试 → 发布私有完整源码和独立静态产物 → 再拉取核对源码。不得在容器工作树内拉取、编辑或提交站点，也不得通过 SSH 修改 blob、数据库或依赖重建容器发布站点。
- 完整源码包括站点测试、构建说明、站点 AGENTS.md；静态产物仅包含浏览器需要的文件。Runtime Data 独立持久化，禁止导出个人数据作为源码 seed 或让重新发布覆盖当前数据。
- 站点“保存版本/提交/推送”应走 skill 发布版本，不能理解为提交容器 Git。平台 Git 更新和容器构建只适用于平台自身变更。
- 平台和站点需求同时出现时，分开目录、验证和发布；不得生成混合提交。若发现混入，阻止该操作并解释边界，优先自动迁到仓库外、通过 skill 完成。只有 slug 或目标服务无法确认时才询问用户。
- 清理历史混入前，必须先保全差异、验证线上源码可恢复；不得直接删除唯一源码、重写 Git 历史或覆盖线上较新版本。
- 提交前运行 `python scripts/check_boundaries.py`；本地启用 `git config core.hooksPath .githooks`。CI 必须通过边界检查，不得跳过或删除守卫来容纳站点代码。
