interface:
  display_name: "Microsite Container"
  short_description: "Deploy static sites and connect scheduled Runtime Data"
  default_prompt: "Use $microsite-container to deploy this static site with an immutable incremental release, and use scoped Runtime Data writers when server-generated data must update without redeploying the frontend."
